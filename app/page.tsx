import { ChatInterface } from "@/components/chat-interface";

const Home = () => {
  return (
    <main className="min-h-screen bg-background">
      <ChatInterface />
    </main>
  );
};

export default Home;
